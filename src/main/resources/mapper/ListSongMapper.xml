<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javaclimb.music.dao.ListSongMapper">
    <resultMap id="BaseResultMap" type="com.javaclimb.music.domain.ListSong" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="song_id" jdbcType="INTEGER" property="songId"/>
        <result column="song_list_id" jdbcType="INTEGER" property="songListId"/>
    </resultMap>

    <resultMap id="BaseResultMap_Song" type="com.javaclimb.music.domain.Song" >
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="singer_id" jdbcType="INTEGER" property="singerId"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="pic" jdbcType="VARCHAR" property="pic"/>
        <result column="url" jdbcType="VARCHAR" property="url"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,song_id,song_list_id
    </sql>

    <insert id="insert" parameterType="com.javaclimb.music.domain.ListSong">
        insert into list_song
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="songId != null">
                song_id,
            </if>
            <if test="songListId != null">
                song_list_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id = #{id},
            </if>
            <if test="songId != null">
                song_id = #{songId},
            </if>
            <if test="songListId != null">
                song_list_id = #{songListId},
            </if>
        </trim>
    </insert>
    
    <update id="update" parameterType="com.javaclimb.music.domain.ListSong">
        update list_song
        <set>
            <if test="songId != null">
                song_id = #{songId},
            </if>
            <if test="songListId != null">
                song_list_id = #{songListId},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteSong" parameterType="com.javaclimb.music.domain.ListSong">
        delete from list_song
        where song_id = #{songId} AND song_list_id = #{songListId}
    </delete>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select *
        from list_song
        where id = #{id}
    </select>

    <select id="allListSong" resultMap="BaseResultMap">
        select *
        from list_song
    </select>

    <select id="selectSongBySongListId" resultMap="BaseResultMap_Song" parameterType="java.lang.Integer">
        SELECT
            s.id AS id,
            s.singer_id As singer_id,
            s.name AS name,
            s.introduction AS introduction,
            s.create_time AS create_time,
            s.update_time AS update_time,
            s.pic AS pic,
            s.url AS url
        FROM
            song s
        JOIN
            list_song ls ON s.id = ls.song_id
        WHERE
            ls.song_list_id = #{songListId}
    </select>
</mapper>